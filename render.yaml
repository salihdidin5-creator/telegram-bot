services:
  - type: web
    name: telegram-bot
    env: python
    plan: free
    region: oregon
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn bot:app
    autoDeploy: true
